<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.SwipeRefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/swipeRefreshLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.constraint.ConstraintLayout
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.mikebl71.android.common.ViewStackPlayer
            android:id="@+id/rainradar_player"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            app:layout_constraintDimensionRatio="600:480"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageButton
            android:id="@+id/rainradar_replay_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/Widget.AppCompat.ImageButton"
            android:src="@drawable/ic_play_arrow_24dp"
            app:layout_constraintBottom_toBottomOf="@id/rainradar_player"
            app:layout_constraintLeft_toLeftOf="@id/rainradar_player"
            app:layout_constraintRight_toRightOf="@id/rainradar_player"
            app:layout_constraintHorizontal_bias="0.75">
            <requestFocus />
        </ImageButton>

    </android.support.constraint.ConstraintLayout>

</android.support.v4.widget.SwipeRefreshLayout>
